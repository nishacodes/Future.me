<h1>Industries</h1>
<% @industries.each do |industry| %>
  <li><%= link_to "#{industry.name}", "/industries/#{industry.id}/companies"%>
    <%= CompanyIndustry.where(:industry_id => industry.id).length %>
  </li>
<% end %>
